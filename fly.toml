app = "weather-bot"
primary_region = "waw"  # Wien, Österreich - NICHT blockiert!

[build]
  dockerfile = "Dockerfile"

[env]
  PYTHONUNBUFFERED = "1"

[processes]
  worker = "python run_continuous.py"

[[services]]
  processes = ["worker"]
